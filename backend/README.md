# Backend - FastAPI + LangGraph

## Structure

- `app/api/` - API routes and schemas
- `app/core/` - Core configurations
- `app/services/` - Business logic (agents, RAG, auth)
- `app/models/` - Data models
- `tests/` - Test suite

## Setup

Instructions will be added in upcoming steps.